#!/usr/bin/env zsh

installations=~/.local/state/updates/yay

if (( $(wc -w $installations | cut -d ' ' -f 1) )); then
    (cat $installations | yay -Syuw --noconfirm -) && echo >$installations
else yay -Syuw --noconfirm
fi

